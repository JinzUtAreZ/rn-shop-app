class CartItem {
  constructor(qty, prodPrice, prodTitle, Amount) {
    this.qty = qty;
    this.prodPrice = prodPrice;
    this.prodTitle = prodTitle;
    this.Amount = Amount;
  }
}

export default CartItem;
